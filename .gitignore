# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/
dist/
build/

# uv
.python-version

# Secrets — never commit
/etc/netwatchm/env
*.env
.env

# GeoIP binary data (large, downloaded separately)
geolite2-city-gzip/

# Logs & data (runtime, not source)
*.log
/var/log/
/var/lib/

# OS
.DS_Store
Thumbs.db

# Local test config and generated reports
netwatchm-test.yaml
/reports/

# Production config (has private IPs, Twingate relays — keep local only)
netwatchm.yaml.production

# Generated installer source bundle (built in CI, not checked in)
netwachmInstall/netwatchm-src.zip
netwachmInstall/netwatchm-bundled/

.vscode/
